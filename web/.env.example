# Vybz Circle Web - Environment Variables

# Supabase (copy from root .env)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API URL (leave empty for same-origin proxy, or set full URL for separate deployment)
VITE_API_URL=
